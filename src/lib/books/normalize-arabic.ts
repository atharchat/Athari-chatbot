// MOCK RAG COMPONENT - TEMPORARY
// This will contain logic to normalize Arabic text (e.g. remove tashkeel).

export const normalizeArabic = (text: string): string => {
  // Mock arabic normalization (strip tashkeel, etc)
  return text.trim();
};
