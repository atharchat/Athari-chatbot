// MOCK RAG COMPONENT - TEMPORARY
// This will hold the text chunking logic for Arabic texts.

export const chunkText = (text: string, maxTokens: number = 500) => {
  // Mock chunking
  return [{ text: "Mock chunk", id: "chunk-1" }];
};
