// MOCK RAG COMPONENT - TEMPORARY
// This will perform real input safety validation eventually.

export const checkInputSafety = async (input: string): Promise<boolean> => {
  // Mock safety check
  // E.g. check for bad words
  return true;
};
