import { Pinecone } from `@pinecone-database/pinecone`; import dotenv from `dotenv`; dotenv.config(); async function test() { const pc = new Pinecone({apiKey: process.env.PINECONE_API_KEY}); const index = pc.index(`salafi-scholar`); try { await index.deleteMany({ filter: { source: { "": "أصول_السنة_لأحمد_بن_حنبل" } } }); console.log(`Deleted`); console.log(await index.describeIndexStats()); } catch(e) { console.error(e); } } test();